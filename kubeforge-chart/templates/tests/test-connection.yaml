apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubeforge-chart.fullname" . }}-test-connection"
  labels:
    {{- include "kubeforge-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kubeforge-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
